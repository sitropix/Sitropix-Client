/** Admin-configured pricing page experiment; `stringValue` on feature_flags. */
export const FEATURE_EXPERIMENT_PRICING_LAYOUT = "experiment_pricing_layout";

export const PRICING_LAYOUT_VARIANTS = Object.freeze(["control", "emphasize_popular", "bold_cta"]);
