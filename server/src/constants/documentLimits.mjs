/** Per-file size cap for support tickets and uploaded documents (bytes). */
export const DOCUMENT_MAX_BYTES = 2 * 1024 * 1024;

/** Max attachments per support ticket (images, PDFs, docs). */
export const SUPPORT_TICKET_MAX_ATTACHMENTS = 5;

/** Max files per project document category during initial project setup. */
export const PROJECT_ASSET_MAX_PER_TYPE_ONBOARDING = 1;

/** Max files per project document category on the project dashboard. */
export const PROJECT_ASSET_MAX_PER_TYPE = 10;
