# Sitropix Portal

Customer + admin portal for Sitropix. Production runs on EC2 (`app.sitropix.com`).

Tip: `autosync` keeps this repo in sync with the EC2 deployment — see `/home/ec2-user/sitropix-autosync/sync.sh`.
